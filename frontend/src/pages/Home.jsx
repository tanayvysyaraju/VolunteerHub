export default function Home(){
  return (
    <div style={{maxWidth:800, margin:"40px auto"}}>
      <h1>Volunteer Portal (MVP)</h1>
      <p>FastAPI + React starter. Go to Events to fetch from the API.</p>
    </div>
  );
}
